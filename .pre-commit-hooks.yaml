- id: amiss
  name: amiss documentation check
  description: Checks staged documentation against the repository tree it describes.
  entry: sh -c 'amiss check --repo . --object-format "$(git rev-parse --show-object-format)" --base "$(git rev-parse HEAD)" --index --profile enforce'
  language: system
  pass_filenames: false
  always_run: true
