[package]
name = "amiss-md"
version.workspace = true
edition.workspace = true
description = "Document parsers for Amiss, pinned to the CommonMark and GFM corpora"
license.workspace = true
repository.workspace = true
exclude.workspace = true

[dependencies]
amiss-wire = { path = "../amiss-wire", version = "0.23.0" }
markdown = "1"

[dev-dependencies]
sha2.workspace = true

[lints]
workspace = true
