[package]
name = "amiss-md"
version.workspace = true
edition.workspace = true
description = "Document parsers for Amiss, pinned to the CommonMark and GFM corpora"
license.workspace = true
repository.workspace = true
# /tests/internal ships so the src include resolves; consumers compile it empty.
exclude = ["/tests/*.rs", "/tests/fixtures", "/benches"]

[dependencies]
amiss-wire = { path = "../amiss-wire", version = "0.21.0" }
markdown = "1"

[dev-dependencies]
sha2.workspace = true

[lints]
workspace = true
