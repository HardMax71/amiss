## Attribute by hash {#hash-id}

## Attribute by pair { id="pair-id" }

## Attribute among classes { .cls #both-id }

## Attribute in the kramdown spelling {: #colon-id }

[](){#empty-link-id}

A paragraph whose last line is an attribute block.
{#standalone-id}

A paragraph ending with an attribute block on its own line. {#trailing-id}

```text
{#inside-a-fence}
```

`{#inside-inline-code}`

A paragraph whose last line is code.
`{#code-on-the-last-line}`

## Heading whose block is code `{id=code-in-a-heading}`
